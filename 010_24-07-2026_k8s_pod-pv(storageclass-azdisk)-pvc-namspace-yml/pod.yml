apiVersion: v1
kind: Pod
metadata:
  name: nginx-pod1
  namespace: dev-ns

spec:
  containers:
  - name: nginx
    image: nginx
    volumeMounts:
    - name: azure-storage
      mountPath: /usr/share/nginx/html
    - name: azure-storage2
      mountPath: /data

  volumes:
  - name: azure-storage
    persistentVolumeClaim:
      claimName: azuredisk-pvc1
  - name: azure-storage2
    persistentVolumeClaim:
      claimName: azuredisk-pvc2


---

apiVersion: v1
kind: Pod
metadata:
  name: nginx-pod2
  namespace: dev-ns

spec:
  containers:
  - name: nginx
    image: nginx
    volumeMounts:
    - name: azure-storage1
      mountPath: /usr/share/nginx/html

  volumes:
  - name: azure-storage1
    persistentVolumeClaim:
      claimName: azuredisk-pvc1