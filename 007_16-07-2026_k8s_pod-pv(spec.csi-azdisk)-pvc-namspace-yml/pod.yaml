apiVersion: v1
kind: Pod
metadata:
  name: nginx-pod-ad
  namespace: nginx-ns1
  labels:
    app: nginx
spec:
  containers:
    - name: nginx-container
      image: nginx:latest
      ports:
        - containerPort: 80
      volumeMounts:
        - name: azure-disk
          mountPath: /usr/share/nginx/html
  volumes:
    - name: azure-disk
      persistentVolumeClaim:
        claimName: azure-disk-pvc